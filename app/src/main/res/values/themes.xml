<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlexClass" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlexClass" parent="Base.Theme.FlexClass" />

    <style name="CustomEditText" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/white</item> <!-- Цвет линии в обычном состоянии -->
        <item name="colorControlActivated">@color/white</item> <!-- Цвет линии при фокусе -->
        <item name="colorControlHighlight">@color/white</item> <!-- Цвет при нажатии -->
    </style>
</resources>